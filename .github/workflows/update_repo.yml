Name: Update Repository

on:
  release:
    types: [published]

# ١. پێویستە مۆڵەت بدەیت بە وەرکفلۆکە بۆ ئەوەی بتوانێت گۆڕانکارییەکان پۆست بکات (Commit & Push)
permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          # ٢. دڵنیابە کە بە Token ی خۆیەوە دێتە ناوەوە بۆ ئەوەی مۆڵەتی هەبێت
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update app-repo.json
        run: |
          chmod +x update-repo.sh
          ./update-repo.sh

      - name: Commit changes
        uses: EndBug/add-and-commit@v9
        with:
          default_author: github_actions
          message: "chore: update repo"
          add: 'app-repo.json'
        env:
          # ٣. لێرەشدا دڵنیابە کە ئەم تۆکنە دەبینێت
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
